#new_wrap
  = form_for @post, url: {action: 'create'} do |f|
    - @tags.each do |t|
      = t.tag_text 
      = check_box_tag "post[t_ids][]", t.id, @post.tag_ids.include?(t.id)
    = render partial: "errors"
    = render partial: "form", locals: {f: f}
  